---
description: List current work-tracking WIP grouped by section
---

Read `{{USER_HOME}}\work-tracking\wip.md` and list the entries grouped by section (In Progress, Todo, Later). Output **only the first line** of each entry — the top-level bullet with `**TICKET**` `[WorkType]` and title. Do **not** include the working directory, status notes, sub-bullets, or any other context lines. Preserve `[WorkType]` tags and the priority order within each section (top = highest).
